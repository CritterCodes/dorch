services:
  mongo:
    image: mongo:7
    container_name: dorch-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - dorch-mongo-data:/data/db

volumes:
  dorch-mongo-data:
